CREATE OR ALTER TRIGGER DELETAPGTO FOR MOVIMENTODETALHE
ACTIVE AFTER UPDATE
POSITION 2
AS
BEGIN
  /* Trigger body */
  if (new.STATUS = 'S') then
  begin
    DELETE FROM PAGAMENTO where CODORIGEM = old.CODDETALHE;
  end
END;
