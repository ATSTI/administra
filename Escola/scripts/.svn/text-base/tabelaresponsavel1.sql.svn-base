set term ^;

CREATE TABLE RESP_ALUNO (
  COD_RESP_ALUNO         INTEGER                NOT NULL primary key 
, COD_RESPONSAVEL        INTEGER                NOT NULL
, RA                     VARCHAR( 10 )          NOT NULL
, TIPO_RESPONSAVEL       CHAR( 1 )              NOT NULL
)^

commit^
