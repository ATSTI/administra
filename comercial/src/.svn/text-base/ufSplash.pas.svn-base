unit ufSplash;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ExtCtrls, EOneInst;

type
  TfSplashPed = class(TForm)
    Panel1: TPanel;
    Label1: TLabel;
    Label2: TLabel;
    Label3: TLabel;
    Label4: TLabel;
    Image1: TImage;
    Label5: TLabel;
    EvOneInstance1: TEvOneInstance;
    procedure FormShow(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  fSplashPed: TfSplashPed;
  varSplashPED : string;

implementation

uses UDm;

{$R *.dfm}

procedure TfSplashPed.FormShow(Sender: TObject);
begin
    varSplashPED := 'PEDAGOGICO';
end;

end.
